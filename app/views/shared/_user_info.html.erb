<section class="user_info">
  <h1>
    <%= gravatar_for(@user, 80) %>
    <%= @user.name %>
  </h1>
  <p>Microposts(<%= @user.microposts.count %>)</p>
</section>
<section>
  <%= render "shared/stats" %>
</section>
